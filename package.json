{
  "name": "monorepo-template",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "bootstrap": "pnpm install && pnpm -w -s build"
  },
  "devDependencies": {
    "turbo": "^1.10.11",
    "typescript": "^5.0.0"
  }
}
